import {defineStore} from 'pinia';import {api} from '../infrastructure/api';
export const useAuthStore=defineStore('auth',{state:()=>({token:localStorage.getItem('stockflow_token'),name:localStorage.getItem('stockflow_name')??'',role:localStorage.getItem('stockflow_role')??''}),actions:{async login(email:string,password:string){const{data}=await api.post('/auth/login',{email,password});this.token=data.token;this.name=data.fullName;this.role=data.role;localStorage.setItem('stockflow_token',data.token);localStorage.setItem('stockflow_name',data.fullName);localStorage.setItem('stockflow_role',data.role)},logout(){localStorage.clear();this.token=null;location.href='/login'}}});
